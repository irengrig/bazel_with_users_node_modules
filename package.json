{
  "name": "fine_grained_user_modules",
  "version": "0.0.1",
  "description": "prototype for using user's node_modules",
  "dependencies": {
    "simple-is": "0.2.0"
  },
  "scripts": {
    "test": "npm install . && bazel build -s //:test_generated_deps"
  }
}
